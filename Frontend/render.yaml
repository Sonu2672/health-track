services:
  - type: web
    runtime: static
    name: healthtrackf
    buildCommand: npm run build
    staticPublishPath: ./dist

    routes:
      - type: rewrite
        source: /*
        destination: /index.html
